name: Hassfest
on:
  push:
  pull_request:
  schedule:
    - cron: "52 0 * * *"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: home-assistant/actions/hassfest@master
